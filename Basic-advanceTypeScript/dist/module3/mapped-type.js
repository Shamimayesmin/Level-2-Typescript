"use strict";
const arrayOfNumbers = [1, 2, 3,];
const arrayOfString = arrayOfNumbers.map((number) => number.toString());
console.log(arrayOfString);
const area1 = { height: 10, width: "22" };
const rectangularArea = {
    height: 10,
    width: 12,
};
